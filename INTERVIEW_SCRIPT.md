# AgentTrace 项目介绍 — 面试话术

> 这不是面试题，是你**主动介绍项目时说的话**。
> 面试官问"介绍一下你最满意的项目"或者"你在做什么项目"时用。
> 语速正常说大概 2-3 分钟。

---

## 完整版（2-3 分钟）

我最近做了一个开源项目叫 **AgentTrace**，是个 AI Agent 的调试工具。

**先说我为什么做这个。**

现在越来越多人在用 Agent——就是让大模型循环思考、调用工具来完成复杂任务。但有个问题：**Agent 是个黑箱**。你给它一个任务，它去调工具、去推理，最后给你一个结果。如果结果不对，你完全不知道是哪个环节出了问题——是它想错了？是工具返回了错误数据？还是上下文太长导致推理能力下降？

传统软件可以看日志、可以打断点，但 Agent 的行为由 LLM 的即时推理决定，同样的问题两次调用可能走完全不同的路径。**日志解决不了这个问题。**

所以我就想：能不能像 Chrome DevTools 调试网页一样调试 Agent？

**这就是 AgentTrace。** 它是一个开源的 Agent 可观测性工具。

**它的工作方式是这样：**

你在你的 Agent 函数上写一行 `@trace_agent` 装饰器，然后在 ReAct 循环里把调 LLM 和调工具的代码换成我们的 `tracer.step()` 和 `tracer.execute()`。就这么简单——大概改三行代码。

改完之后，你的 Agent 每次执行的每一步都会被自动记录下来：它在想什么、它考虑了哪些工具、为什么选了这个不选那个、调工具花了多少毫秒、消耗了多少 Token。这些数据会形成一个结构化的 **Trace** 对象。

然后这个 Trace 会传到我们后端的 Engine 服务，存在 SQLite 里。你打开浏览器访问 Dashboard，就能看到一个**彩色的决策树**——蓝色节点代表"思考"，绿色节点代表"行动"，黄色节点代表"观察"。每个节点点进去能看到完整的思考内容、候选工具列表、被否决的方案。

还有一个我比较自豪的功能叫 **Debug Agent**。它是一个元 Agent——就是"用来分析其他 Agent 的 Agent"。它有两种分析模式：第一种是纯规则检测，不需要调 LLM，1 毫秒就能检测出 Agent 有没有死循环、工具调用失败、低置信度这些问题。第二种是把检测到的问题发给 LLM，生成详细的诊断报告，告诉你"根因是什么、怎么修、综合评分多少分"。

**技术上，** 这个项目分三层：Python SDK 负责数据采集，FastAPI 后端负责存储和查询，React 前端负责可视化。SDK 里用了一些有意思的设计——比如用 `contextvars` 做隐式上下文传递来解决并发问题，用上下文管理器做自动计时，用 Pydantic 做全链路类型校验。

项目已经开源在 GitHub 上，53 个源文件，八千多行代码。这是仓库地址：`github.com/jiamoonyue/AgentTrace`。

---

## 精简版（1 分钟）

我做了个开源的 AI Agent 调试工具叫 AgentTrace。

现在的 Agent 开发有个痛点：你给了任务，它去推理、调工具、出结果。但如果结果错了，你不知道是哪个环节出的问题——因为 Agent 的行为是 LLM 即时推理决定的，不是固定的代码逻辑。

AgentTrace 做的事情就是：**把 Agent 的每一步决策录下来**。你在代码上加一行装饰器，下次运行 Agent 时，每一步的思考内容、工具选择、耗时、Token 都会被自动记录。然后这些数据会变成一个可视化的决策树，你可以看到 Agent 完整的心路历程——它想了什么、选了哪个工具、为什么没选另一个、花了多少时间。

另外我还做了一个 Debug Agent——用 AI 来分析 Agent 的执行记录，自动检测死循环、工具失败这些问题，并给出修复建议。

技术栈是 Python SDK + FastAPI 后端 + React 前端。已经开源在 GitHub。

---

## 关键词总结

> **一句话定位**：AgentTrace 是 AI Agent 的 Chrome DevTools。
>
> **三个价值点**：
> 1. 一行代码接入，自动录下 Agent 的每一步决策
> 2. 可视化决策树，点开节点看到完整的心路历程
> 3. Debug Agent 自动分析轨迹，检测问题并给修复建议
>
> **技术亮点**：`contextvars` 隐式上下文传递、上下文管理器自动计时、Pydantic 全链路类型校验、元 Agent 诊断架构
